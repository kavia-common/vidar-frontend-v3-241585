<script lang="ts">
  /**
   * Estate Overview — /
   *
   * BUILD INSTRUCTIONS FOR KAVIA:
   * See frontend/UI_REFERENCE.md → "Screen 1 — Estate Overview"
   *
   * API: GET /api/internal/governance/estate-health
   * Type: ApiEstateHealth (from $lib/types/api.types)
   * Schema: ApiEstateHealthSchema (add to $lib/schemas/api.schemas.ts)
   * Query: createQueryFn('/api/internal/governance/estate-health', ApiEstateHealthSchema)
   *
   * Layout: three MetricCard sections — Systems / Last 30 Days / Advisory
   * No user interaction beyond a Refresh button.
   * No governance logic. Display backend data only.
   */
</script>

<div class="p-6 max-w-6xl">
  <p class="text-slate-500 text-sm font-mono">Estate Overview — to be implemented</p>
</div>
